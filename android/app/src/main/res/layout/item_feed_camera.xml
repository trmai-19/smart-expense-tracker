<?xml version="1.0" encoding="utf-8"?>
<!--
  Locket Timeline — Page 0: Live Camera View
  - 3:4 Rounded Viewfinder Card (Corner Radius 28dp)
  - Camera Controls: Gallery (Left) | Shutter (Center) | Flip (Right)
  - Sleek text hint: "Lịch sử"
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_main"
    android:paddingStart="@dimen/spacing_lg"
    android:paddingTop="12dp"
    android:paddingEnd="@dimen/spacing_lg"
    android:paddingBottom="@dimen/spacing_xs">

    <!-- 3:4 Viewfinder Card with Rounded Corners (Locket-style) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_viewfinder"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:cardBackgroundColor="@color/bg_surface"
        app:cardCornerRadius="@dimen/corner_camera"
        app:cardElevation="0dp"
        app:layout_constraintDimensionRatio="H,3:4"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:strokeColor="@color/input_border"
        app:strokeWidth="1.5dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <!-- CameraX Preview View -->
            <androidx.camera.view.PreviewView
                android:id="@+id/preview_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:implementationMode="compatible"
                app:scaleType="fillCenter" />

            <!-- Fallback placeholder if camera not started -->
            <ImageView
                android:id="@+id/iv_camera_placeholder"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/bg_surface"
                android:contentDescription="@null"
                android:scaleType="centerCrop"
                android:visibility="gone" />

            <!-- Hint text inside viewfinder -->
            <TextView
                android:id="@+id/tv_camera_hint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:alpha="0.45"
                android:text="Chụp ảnh món ăn hoặc hóa đơn"
                android:textColor="@color/text_secondary"
                android:textSize="@dimen/text_sm" />
        </FrameLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Camera Controls: Gallery (Left) | Shutter (Center) | Flip (Right) -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layout_controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        app:layout_constraintBottom_toTopOf="@id/tv_history_hint"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/card_viewfinder">

        <!-- Gallery / Photo Upload Button -->
        <ImageButton
            android:id="@+id/btn_gallery"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_circle_button"
            android:contentDescription="Tải ảnh lên"
            android:padding="12dp"
            android:src="@drawable/ic_gallery"
            app:layout_constraintBottom_toBottomOf="@id/layout_shutter"
            app:layout_constraintEnd_toStartOf="@id/layout_shutter"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@id/layout_shutter" />

        <!-- Shutter Button: Sleek 68dp outer ring + 52dp inner solid circle -->
        <FrameLayout
            android:id="@+id/layout_shutter"
            android:layout_width="68dp"
            android:layout_height="68dp"
            android:clickable="true"
            android:focusable="true"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <View
                android:id="@+id/view_shutter_ring"
                android:layout_width="68dp"
                android:layout_height="68dp"
                android:background="@drawable/ic_shutter_outer" />

            <View
                android:id="@+id/btn_shutter_inner"
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:layout_gravity="center"
                android:background="@drawable/ic_shutter_inner" />
        </FrameLayout>

        <!-- Flip Camera Button -->
        <ImageButton
            android:id="@+id/btn_flip_camera"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_circle_button"
            android:contentDescription="Quay camera"
            android:padding="12dp"
            android:src="@drawable/ic_flip_camera"
            app:layout_constraintBottom_toBottomOf="@id/layout_shutter"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/layout_shutter"
            app:layout_constraintTop_toTopOf="@id/layout_shutter" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Sleek "Lịch sử" hint button at bottom (Taps or swipes up to go to history) -->
    <TextView
        android:id="@+id/tv_history_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:background="?android:attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:paddingStart="16dp"
        android:paddingTop="6dp"
        android:paddingEnd="16dp"
        android:paddingBottom="6dp"
        android:text="Lịch sử  ↑"
        android:textColor="@color/text_secondary"
        android:textSize="@dimen/text_sm"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/layout_controls" />

</androidx.constraintlayout.widget.ConstraintLayout>
